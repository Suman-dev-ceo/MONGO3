<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Wattsapp</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    
    <h2>All Chats!</h2>
    <form method="GET" action="/chats/new">
        <button>New Chat</button>
    </form>
    <%for(let chat of chats){ %>
    <div class="chat">
        <p>From: <i><%= chat.from %></i></p>
        <div class="msg">
            <p>Message: <%= chat.msg %></p>
            <form class="btn" method="GET" action="/chats/<%=chat._id%>/edit">
                <button>Edit</button>
            </form>
            <form class="btn" method="POST" action="/chats/<%=chat._id%>?_method=DELETE">
                <button>Delete</button>
            </form>
        </div>
        <p>To: <i><%=chat.to%></i></p>
        <hr>
        <div class="time">
            <div class="createdAt">Created at:
                <p><%=chat.created_at.toString().split(' ')[4] %></p>
                <p><%= chat.created_at.toString().split(' ').slice(0,4).join(' ') %>  </p>
            </div>
            <div class="modifiedAt">Modified at:
                <p><%= chat.modified_at.toString().split(' ')[4]%></p>
                <p><%= chat.modified_at.toString().split(' ').slice(0,4).join(' ')  %>  </p>
            </div>
        </div>
    </div>
    
<%}%>
</body>
</html>

